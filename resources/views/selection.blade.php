<!-- resources/views/role-selection.blade.php -->

        <div class="text-center">
            <h2>Pick your role.</h2>
            <a href="{{ route('athlete-register') }}" class="btn btn-primary">Athlete</a>
            <a href="{{ route('coach-register') }}" class="btn btn-primary">Coach</a>
            <a href="{{ route('stadium-register') }}" class="btn btn-primary">Stadium Owner</a>
        </div>

